-- This is a site configuration for Gluon v2019.1
--
{
  domain_names = {
    boy = 'Freifunk Bad Oyenhausen'
  },

  domain_director = {
    url = 'http://setup.ipv6.4830.org/dd.php',
    switch_after_offline = 360,
    enabled = true,
  },

  geolocator = {
    url = 'http://setup.4830.org/geolocator.php'
  },

  -- 32 bytes of random data, encoded in hexadecimal
  -- This data must be unique among all sites and domains!
  -- Can be generated using: echo $(hexdump -v -n 32 -e '1/1 "%02x"' </dev/urandom)
  -- Generated by »echo "Freifunk Bad Oyenhausen Reboot 2021" | awk 'BEGIN{for(n=0; n<256; n++) ord[sprintf("%c",n)]=n;} {newstr=""; for(i=1; i<33; i++) {if(i>length($0)) c="\0"; else c=substr($0, i, 1); newstr=sprintf("%s%02x", newstr, ord[c]);} printf("%-64.64s", newstr)}' ; echo«
  domain_seed = '4672656966756e6b20426164204f79656e68617573656e205265626f6f742032',

  -- Prefixes used within the mesh.
  -- prefix6 is required, prefix4 can be omitted if next_node.ip4
  -- is not set.
  prefix6 = 'fd10:ca1:b0e::/64',

  -- List of NTP servers in your community.
  -- Must be reachable using IPv6!
  ntp_servers = {'ntp.4830.org', 'ntp.services.ffgt.net'},

  -- Wireless configuration for 2.4 GHz interfaces.
  wifi24 = {
    -- Wireless channel.
    channel = 1,

    -- ESSID used for client network.
    ap = {
      ssid = 'badoeynhausen.freifunk.net',
      -- disabled = true, -- (optional)
    },

    mesh = {
      id = 'mesh_v15',
      -- id = 'boy-mesh-intern',
      mcast_rate = 12000,
      -- disabled = true, (optional)
    },
  },

  -- Wireless configuration for 5 GHz interfaces.
  -- This should be equal to the 2.4 GHz variant, except
  -- for channel.
  wifi5 = {
    channel = 36,
    outdoor_chanlist = "100-140",
    htmode = 'VHT40',
    ap = {
      ssid = 'badoeynhausen.freifunk.net',
      -- disabled = true, -- (optional)
    },

    mesh = {
      -- id = 'mesh_v15',
      id = 'boy-mesh-intern',
      mcast_rate = 12000,
      -- disabled = true, (optional)
    },
  },

  mesh = {
    batman_adv = {
      routing_algo = 'BATMAN_IV',
      gw_sel_class = 1,
    },
    vxlan = false,
  },

  -- The next node feature allows clients to always reach the node it is
  -- connected to using a known IP address.
  next_node = {
    ip6 = 'fd10:ca1:b0e::1',
  },

  mesh_vpn = {
    -- 1364 taken from Freifunk Muenster
    mtu = 1364,

	tunneldigger = {
	  brokers = {
	    'g01m14.4830.org:10014',
	    'g02m14.4830.org:10014',
	    'g03m14.4830.org:10014',
	    'g04m14.4830.org:10014',
	  },
    },
  },
}
